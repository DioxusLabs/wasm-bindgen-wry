# Rebuild status: pull-out-runtime onto origin/main (#27/#28) — COMPLETE (driver port)

Worktree: `/Users/evanalmloff/Desktop/wasm-bindgen-wry-rebuild`
Branch: `pull-out-runtime-rebuild` (parent = origin/main `f483b7b`; #27/#28 in ancestry)
Original branch preserved at `pull-out-runtime`; pre-merge backup at `pre-merge-backup`.

Strategy chosen: **A — preserve your refactor**, with the clarification that the
**IPC transport uses origin's driver model** (the proxy bridge is gone).

## Result
- Whole workspace compiles (abi/core/runtime/shim/macro-support/wry-launch).
- All crate unit tests pass: 28 runtime (incl. driver `WryIPC` + `wry` protocol-handler
  tests), abi, core.
- `wry-launch` integration `main_thread_tests`: **220 pass / 8 fail**. The 8 failures are
  all `jsvalue::test_instanceof_*` (has_type/is_instance_of/dyn_into/dyn_ref for Array/Date)
  — these are fixed by your uncommitted working-tree `instanceof` work (BinaryEncode/
  CallbackKey), NOT a rebuild regression. They'll pass once that WIP lands.

## How the event-loop core was ported (driver into your idioms)
- `runtime.rs` = origin's driver transport (`WryIPC { slots, commands }`, `DriverCommand`
  queue, `IPCSingleSlots`, `AtomicWaker`) + YOUR callback-dispatch bottom half
  (`u32::decode`, `inventory::iter::<JsExportSpec>`, abi-public API). Idiom adaptations:
  `OutboundIPCMessage` → `IPCMessage`, bare `respond_encoder`, `finish_respond_message -> IPCMessage`.
- `batch.rs` = your branch version (EncodedParts refactor) verbatim; only the test helper
  retargeted to driver `WryIPC::new()`.
- `wry.rs` = origin's driver `WryBindgen`/`split`/`WryBindgenWebviewDriver`/`ProtocolHandler`;
  `OutboundIPCMessage` → `IPCMessage` (6 sites); test helpers use `crate::ipc::empty_message`.
- `lib.rs` exports the driver API (`WryBindgen, WryBindgenDriver, WryBindgenRuntime,
  WryBindgenWebviewDriver, ProtocolHandler`) instead of `WryBindgenEvent`/`AppBuilder`.
- `wry-launch`: `WryBindgen`/`WryBindgenWebviewDriver`/`batch`/`batch_async` repointed to
  `wry_bindgen_runtime`; macro/`JsValue`/`Closure` stay on the `wasm_bindgen` shim.
- `wry-bindgen-runtime/Cargo.toml`: added `atomic-waker`.
- js-sys: brought your `queue.rs`/`lib.rs` (1-line each; the 2-arg thread-local API);
  `futures/mod.rs` was already byte-identical to origin.

## Remaining for you
1. Re-apply / commit your in-progress `instanceof` fix → the 8 tests pass.
2. Review the driver port (esp. `runtime.rs`/`wry.rs`) — origin's concurrency logic is intact,
   only paths/idioms changed; worth a read since it's the event-loop core.
3. `Cargo.lock` regenerated by the build; `js/{hash.txt,main.js}` regenerated from TS.
